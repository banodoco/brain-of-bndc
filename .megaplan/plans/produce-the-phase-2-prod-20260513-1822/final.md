# Execution Checklist

- [x] **T1:** Verify output path is unused and re-confirm all source anchors before authoring. Check that docs/live-update-editor-phase-2-runbook.md does not already exist; re-read the named line ranges in docs/live-update-editor-redesign.md (117-123, 188-191, 212-223, 596, 599-604, 611, 627, 633, 642-644, 646-653, 651, 684), docs/runbook-payments.md (1-92, 286-407), src/common/storage_handler.py (652-682, 652-1968), src/features/summarising/live_update_editor.py (60, 84-107, 261-346), and .migrations_staging/README.md to confirm the hand-off destination path /Users/peteromalley/Documents/banodoco-workspace/supabase/migrations/. Note the absence of any run_source/editor_source/run_type column on live_update_editor_runs (this confirms the sixth TODO(phase-1) needs to be added).
  Executor notes: Verified docs/live-update-editor-phase-2-runbook.md does not exist (ls returned NOT_FOUND). Re-read redesign anchors :110-223 and :580-697 — confirmed publication_status enum (pending|sent|partial|failed|null) on topics :118, indexes :130-132 + :189-191, old-to-new mapping :212-223 (six legacy table names: live_update_feed_items, live_update_watchlist, live_update_duplicate_state, live_update_editorial_memory, live_update_candidates, live_update_decisions), trip-wires :599-604 (partial-publish ×2 → restore topic_publications; override >10% → tune; record_observation <1/run → drop tool), Phase-1 publishing-OFF at :611/:627/:633, Phase-2 plan :646-653 with dual-write continuing on legacy tables :651, lease key (environment, guild_id, live_channel_id) at :684, runner-pointer-flip rollback + _legacy_* 2+ weeks at :596. Re-read storage_handler.py:652-682 — live_update_editor_runs has guild_id, environment, trigger, status, live_channel_id, checkpoint_before/after, memory_snapshot, duplicate_snapshot, candidate_count, accepted_count, rejected_count, duplicate_count, deferred_count, skipped_reason, error_message, metadata, completed_at; NO run_source / editor_source / run_type column. Sixth TODO(phase-1) confirmed required. Re-read live_update_editor.py:60 (DEFAULT_MAX_PUBLISH_PER_RUN=None), :84-107 (init), :261-346 (run_once → _select_publishable_candidates → _publish_accepted_candidates as legacy publish path). Re-read .migrations_staging/README.md — destination /Users/peteromalley/Documents/banodoco-workspace/supabase/migrations/ confirmed plus copy-verbatim-filenames-unchanged convention. Re-read runbook-payments.md:1-92 for tone reference (terse imperative SQL-first triage with decision tables).
  Reviewer verdict: Pass. Audit confirmed output path was unused, anchors re-read, and the absent run-source discriminator column on live_update_editor_runs flagged as the sixth TODO(phase-1).
  Evidence files:
    - docs/live-update-editor-phase-2-runbook.md

- [x] **T2:** Author §1 (header, audience, scope, provisional banner) and §2 (Fast Triage SQL) of docs/live-update-editor-phase-2-runbook.md. §1: H1 + 3-5 line preamble naming oncall as audience, after-Phase-1-gate as timing, what the doc covers/doesn't (point at redesign for Phase 1 design). Provisional banner enumerates SIX TODO(phase-1) items: (a) toggle name + location, (b) suppression mechanism Branch A scheduler-tick vs Branch B publish-call, (c) Branch-B run-row suppression marker (column/metadata key if any), (d) new-editor / legacy bot identity in the live Discord channel, (e) prod trace-embed channel, (f) legacy/new run-source discriminator on live_update_editor_runs (column, metadata JSON key, or trigger-prefix convention — table today per src/common/storage_handler.py:652-682 has no such column). Explicit out-of-scope clause: if Phase 1 ships any third mechanism (hybrid, per-guild config, per-channel rollout, partial guild rollout) OR omits the run-source discriminator → ABORT to consult-the-doc escape hatch; do not improvise. Primary tools bullet list (<LIVE_EDITOR_PUBLISH_TOGGLE> placeholder, trace channel TODO(phase-1), SQL one-liners in this doc, legacy editor entrypoint at src/features/summarising/live_update_editor.py:60 + publish wiring :84-107/:261-346, migration hand-off path). Guardrails (rename only after watch; cross-window trip-wire; both editors keep writing their own tables; default ABORT). §2: Fast Triage SQL block scoped environment='prod' grouping by publication_status over last hour, with `-- swap to 'dev' for pre-flip verification (note: dev rows are diagnostic only; no 'partial' can appear in dev during Phase 1 per :611/:627/:633)`. 'Then classify' bullet list per status with jump-to pointers. State-summary one-liner (`select state, count(*) from topics where environment='prod' group by state;`). 'Recent runs' one-liner against live_update_editor_runs with note that Branch-B suppression marker AND run-source discriminator appear here — both TODO(phase-1).
  Depends on: T1
  Executor notes: Wrote H1 + 5-line preamble naming oncall, after-Phase-1-gate timing, covers/doesn't list pointing at redesign for Phase 1 design. §1 banner enumerates SIX TODO(phase-1) items: (a) toggle name + location with <LIVE_EDITOR_PUBLISH_TOGGLE>, (b) Branch A vs Branch B suppression mechanism, (c) Branch-B run-row suppression marker column/metadata key, (d) new-editor + legacy bot identities in live channel, (e) prod trace-embed channel, (f) run-source discriminator on live_update_editor_runs with explicit note that storage_handler.py:652-682 confirms no run_source/editor_source/run_type column today. Out-of-scope clause: any third mechanism (hybrid, per-guild, per-channel, partial-guild) OR omission of TODO(f) → ABORT to consult-the-doc (§11). Primary tools bullet list covers <LIVE_EDITOR_PUBLISH_TOGGLE>, trace channel, SQL one-liners, legacy editor entrypoint at live_update_editor.py:60, legacy publish wiring _publish_accepted_candidates :84-107 + _select_publishable_candidates :261, migration hand-off chain. Guardrails: rename only after watch, cross-window trip-wire citing :600-601, both editors keep writing own tables citing :651, default ABORT. §2 Fast Triage SQL groups by publication_status scoped environment='prod' last-hour, includes -- swap to 'dev' for pre-flip verification comment with 'no partials in dev during Phase 1 per :611/:627/:633 — 0 dev partials does NOT mean prod is clean' annotation. 'Then classify' bullet list per status with jump-to pointers to §6/§7. State-summary one-liner against topics. Recent-runs one-liner against live_update_editor_runs noting Branch-B marker (TODO c) AND run-source discriminator (TODO f) appear on those rows.
  Reviewer verdict: Pass. §1 banner enumerates SIX TODO(phase-1) items with explicit third-mechanism abort clause; §2 Fast Triage SQL scopes environment='prod' with the dev-swap comment and the no-partials-in-dev annotation.
  Evidence files:
    - docs/live-update-editor-phase-2-runbook.md

- [x] **T3:** Author §3 (pre-flip preconditions checklist) and §4 (shadow-mode → cutover GO/TUNE/ABORT rubric). §3: GATE — verification — pass-criterion lines for: Phase 1 soak passed (:642-644); 5-table schema migration applied; named indexes present (topics_state_idx, topics_revisit_idx, topics_headline_trgm, transitions_* per :188-191); backfill row-count parity (live_update_feed_items vs topics WHERE state='posted'; live_update_watchlist vs state='watching'); runner lease key in place (:684); trace embed wired in prod monitoring channel (:611-644); legacy editor still running (latest live_update_editor_runs row under legacy run-source within last hour); operator paged-in window booked; ALL SIX Phase-1 TODOs resolved (toggle, suppression mechanism, Branch-B marker, bot identity, trace channel, run-source discriminator); Phase 1 actually shipped Branch A or Branch B (not a third mechanism — if third mechanism OR run-source discriminator absent, abort cutover). End: 'If any gate is RED, do not flip. Consult docs/live-update-editor-redesign.md and reschedule.' §4: GO/TUNE/ABORT table keyed on Phase-1 signals from :611-644 and :599-604 — replay-divergence explainability; collision-override rate (<10% GO / 10-25% TUNE / >25% ABORT per :603); record_observation rate (1-3/run GO / <1/run TUNE per :602); Phase-1 shadow-attempted-publish partials from shadow telemetry (0 GO / 1 TUNE — counts toward cross-window trip-wire); schema-rejection rate from topic_transitions WHERE action LIKE 'rejected_%' (<10%/10-50%/>50%); cost/run; latency/run. Annotation on partial-publish row: 'Sourced from Phase-1 shadow telemetry (trace embed in Phase-1 monitoring channel + shadow logs per :611-644), NOT from topics WHERE environment='dev' — Phase 1 runs with publishing OFF per :611/:627/:633.' Define TUNE = delay flip + consult redesign; ABORT = walk away from Phase 2 this week.
  Depends on: T2
  Executor notes: §3 authored as a 12-row Gate|Verification|Pass-criterion markdown table covering: Phase 1 soak passed (:642-644); 5-table schema applied; named indexes present per :130-132+:189-191 (topics_state_idx, topics_revisit_idx, topics_headline_trgm, topic_sources_message_idx, transitions_topic_idx, transitions_run_idx, transitions_action_idx); backfill row-count parity for state='posted' vs live_update_feed_items and state='watching' vs live_update_watchlist; runner lease key per :684; trace embed in prod TODO(phase-1) e channel; legacy editor still running (latest live_update_editor_runs row under <run-source discriminator = legacy> within last hour); operator paged-in window; ALL SIX Phase-1 TODOs resolved (toggle, suppression mechanism, Branch-B marker, bot identities, trace channel, run-source discriminator); Phase 1 shipped Branch A or Branch B (not hybrid/per-guild/per-channel/partial-guild); run-source discriminator present (sample SELECT shows distinct legacy/new). End-statement: 'If any gate is RED, do not flip. Consult docs/live-update-editor-redesign.md and reschedule.' Explicit ABORT-to-§11 for third-mechanism or missing discriminator. §4 authored as Signal|GO|TUNE|ABORT table keyed on Phase-1 signals from :611-644/:599-604: replay-divergence explainability; collision-override rate <10%/10-25%/>25% citing :603; record_observation rate 1-3/run / <1/run TUNE citing :602; Phase-1 shadow-attempted-publish partials 0 GO / 1 TUNE (counts toward cross-window trip-wire) / ≥2 ABORT citing :600-601; schema-rejection rate <10%/10-50%/>50% citing :676; cost/run ≤3× / 3-5× / >5× citing :690; latency/run scheduler-cadence-aware citing :633. Annotation above the table: 'Sourced from Phase-1 telemetry (trace embed + shadow logs), NOT from topics WHERE environment=dev — Phase 1 runs with publishing OFF per :611/:627/:633.' Decision rules: GO=proceed to §5; TUNE=delay flip + consult redesign for collision threshold/rejection-rate/cost; ABORT=walk away from Phase 2 this week and re-open redesign.
  Reviewer verdict: Pass. §3 precondition table includes both 'Phase 1 shipped Branch A or Branch B' and 'Run-source discriminator present' as separate gates; §4 rubric annotates partial-publish row as sourced from Phase-1 telemetry NOT topics WHERE environment='dev'.
  Evidence files:
    - docs/live-update-editor-phase-2-runbook.md

- [x] **T4:** Author §5 (publishing-flag-flip procedure) and §6 (partial-publish detection SQL). §5: Branch A (scheduler-tick gate) verification — primary signal = no new live_update_editor_runs rows under the legacy run-source after the flip timestamp; new editor's run row appears on the next tick. ADD ONE-LINE NOTE: 'requires Phase-1-provided run-source discriminator (sixth TODO); if absent at flip time, treat as third-mechanism case and ABORT.' Branch B (publish-call gate at _publish_accepted_candidates :84-107 / _select_publishable_candidates :261-346) verification — PRIMARY signal = Discord-side count: operator scrolls the live Discord channel since the flip timestamp and confirms zero posts attributed to the legacy bot identity. SECONDARY, nice-to-have: if Phase 1 writes a suppression marker on the legacy live_update_editor_runs row, an SQL check on that column/metadata key (TODO(phase-1)). If no marker shipped, Discord-side count is the only signal — acceptable. Procedure body (8 steps): re-run §3 checklist; set <LIVE_EDITOR_PUBLISH_TOGGLE> to Phase-1-chosen suppression value; record flip timestamp (watch start); ANCHOR VERIFICATION ON NEXT OBSERVED RUN (not fixed window — cadence hourly per :633) — poll `select run_id, status, completed_at from live_update_editor_runs where environment='prod' and created_at > '<flip timestamp>' order by created_at asc;` until a NEW row appears with status='completed'; verification SQL one-liner against topics scoped environment='prod' with last_published_at > flip timestamp grouped by publication_status; confirm ≥1 'sent', zero 'partial', zero unexpected 'failed'; confirm branch-appropriate signal from step 1; if any check fails, jump to §8. EXPLICITLY DO NOT check for 'zero new rows in legacy live_update_feed_items'. §6: four query blocks scoped environment='prod' with `-- swap to 'dev' for pre-flip verification (note: dev has no partials during Phase 1)` annotations — (a) windowed counts: last_hour/last_24h/since_cutover for publication_status='partial'; (b) per-row deep-dive selecting id, canonical_key, headline, state, publication_status, publication_error, publication_attempts, last_published_at, discord_message_ids ordered by last_published_at desc limit 50; (c) still-pending-too-long: publication_status='pending' AND (last_published_at IS NULL OR last_published_at < now() - interval '90 minutes'); (d) rejection-rate against topic_transitions WHERE action LIKE 'rejected_%' last 1h grouped by action. Two-source trip-wire tally: prod-only SQL count `select count(*) as prod_partials_since_cutover from topics where environment='prod' and publication_status='partial' and last_published_at >= '<cutover-flip-timestamp>';` followed by explicit instruction: 'Add the Phase-1 shadow-attempted-publish partial count from shadow-mode telemetry (trace embed + shadow logs per :611-644) — NOT from topics. Record both numbers inline: Phase-1 shadow partials (from telemetry): ____ ; Phase-2 prod partials (from query above): ____ ; Trip-wire fires when sum ≥ 2 per :600-601: ABORT + restore topic_publications.' Annotation: 'See :117-123, :223 for column shapes.'
  Depends on: T3
  Executor notes: §5: Branch A verification names primary signal as zero new live_update_editor_runs rows under the legacy run-source after the flip timestamp + new run-source row on next tick, with the required one-line note 'requires Phase-1-provided run-source discriminator (TODO(phase-1) f); if absent at flip time, treat as third-mechanism case and ABORT per §1.' Branch B verification names Discord-side count as PRIMARY (operator scrolls live channel for window flip-to-now, confirms zero legacy-bot-identity posts per TODO(phase-1) d); SQL marker on legacy live_update_editor_runs row demoted to SECONDARY nice-to-have (TODO(phase-1) c), with explicit fallback 'If no marker shipped, the Discord-side count is the only signal — that is acceptable.' Cites src/features/summarising/live_update_editor.py:84-107 _publish_accepted_candidates and :261 _select_publishable_candidates as the legacy fallback path. Explicit DO-NOT statement: 'Do not use "zero new rows in legacy live_update_feed_items" as a verification signal. Per :651, legacy table writes continue during the watch; only Discord-send is suppressed.' Procedure body has 8 numbered steps: (1) re-run §3; (2) set <LIVE_EDITOR_PUBLISH_TOGGLE> to TODO(phase-1) a value; (3) record flip timestamp as watch start and lower bound for subsequent SQL; (4) ANCHOR VERIFICATION ON NEXT OBSERVED RUN with polling SELECT against live_update_editor_runs where created_at > <flip timestamp>, waiting until status='completed' appears (~90 min max per hourly :633 cadence, jump to §8 otherwise); (5) verification SQL one-liner against topics scoped environment='prod' with last_published_at > flip timestamp grouped by publication_status; (6) confirm ≥1 'sent', zero 'partial', zero unexpected 'failed', jump to §6+§7 partial row on any partial; (7) confirm branch-appropriate signal — Branch A repeats step-4 query projecting the run-source discriminator (zero rows under legacy after flip), Branch B Discord-side scroll with SQL marker secondary; (8) any failure → §8, otherwise enter §7 watch. §6: four query blocks scoped environment='prod' with `-- swap to 'dev' for pre-flip verification (note: dev has no partials during Phase 1 per :611/:627/:633 — 0 dev partials does NOT mean prod is clean)` annotations — (a) UNION-ALL windowed counts for last_hour/last_24h/since_cutover on publication_status='partial'; (b) per-row deep-dive selecting topic_id, canonical_key, headline, state, publication_status, publication_error, publication_attempts, last_published_at, discord_message_ids ordered by last_published_at desc limit 50; (c) still-pending too long with NULL or older-than-90-minutes last_published_at; (d) rejection-rate against topic_transitions WHERE action LIKE 'rejected_%' last 1h grouped by action. Two-source trip-wire tally: prod-only SQL count `select count(*) as prod_partials_since_cutover from topics where environment='prod' and publication_status='partial' and last_published_at >= '<cutover-flip-timestamp>';` then explicit instruction 'Add the Phase-1 shadow-attempted-publish partial count from shadow-mode telemetry (trace embed in the Phase-1 monitoring channel + shadow logs per :611-644) — NOT from topics.' with the reasoning 'topics WHERE environment=dev AND publication_status=partial is vacuous; the shadow-attempted-publish counter is the only Phase-1 source.' Three operator-filled blanks (Phase-1 shadow partials, Phase-2 prod partials, sum). Trip-wire phrasing: 'fires when sum ≥ 2 per :600-601 → ABORT (§8) + restore topic_publications per :601.' References §7 partial-publish row for the same threshold. Schema-shape pointer: 'Column shapes per docs/live-update-editor-redesign.md:117-123 and :223.'
  Reviewer verdict: Pass. §5 Branch B labels Discord-side count PRIMARY and SQL marker secondary; anchors verification on next observed run; explicit ban on 'zero new rows in legacy live_update_feed_items' check. §6 uses prod-only SQL count + explicit Phase-1 telemetry instruction.
  Evidence files:
    - docs/live-update-editor-phase-2-runbook.md

- [x] **T5:** Author §7 (24-48h watch decision tree) and §8 (rollback procedure — runner-pointer flip). §7: SYMPTOM | THRESHOLD | ACTION | POINTER markdown table. Rows: publication_status='partial' — 1 in cutover window → INVESTIGATE; (Phase-1 telemetry shadow partials + Phase-2 prod partials from §6) cumulative ≥ 2 per :600-601 → ABORT + restore topic_publications + §8. Rejection rate >50%/hr from §6(d) → TUNE/ABORT consult redesign. Collision-override rate >10% → TUNE per :603. Duplicate posts to live channel: 1 isolated → HOT-FIX (check _publish_accepted_candidates idempotency / topic_sources uniqueness per :625); recurring → ABORT. Legacy editor accidentally publishes to live channel → HOT-FIX: re-verify toggle + branch-A/B signal from §5. Discord 429s/auth errors from new editor → HOT-FIX routing. THREE SEPARATE LEASE ROWS per :684: new-editor self-contention → HOT-FIX release lease for (environment, guild_id, live_channel_id) + verify next tick; legacy editor writing to NEW tables (topics, topic_*) → ABORT (isolation broke); new + legacy run rows overlapping in same lease key → ABORT (lease misconfigured). Shape divergence vs Phase 1 baseline: explainable → ACCEPT; unexplained → TUNE/ABORT. record_observation <1/run sustained → INFO (post-cutover concern per :602). Phase 1 turns out to be a third mechanism (not A or B) OR run-source discriminator absent → ABORT immediately; runbook does not cover. End: 'If symptom not listed, ABORT to rollback and consult docs/live-update-editor-redesign.md.' §8: Pre-rename rollback (common case during watch): (1) Flip <LIVE_EDITOR_PUBLISH_TOGGLE> to legacy value; (2) Drain new-editor in-flight run OR force lease release for (environment, guild_id, live_channel_id) per :684; (3) Wait for next scheduler tick; confirm legacy editor publishes via _publish_accepted_candidates (src/features/summarising/live_update_editor.py:84-107) — Branch A: new legacy live_update_editor_runs row appears under legacy run-source; Branch B: existing legacy run rows now show suppression marker CLEARED (if Phase 1 writes one) AND new live-channel post attributed to legacy bot identity (primary Discord-side signal); (4) Verify new row in live_update_feed_items for prod run AND corresponding Discord post; (5) Leave topics rows alone — do not truncate or rewrite. Cite :596. Post-rename rollback variant (rare): same toggle flip + reverse-rename SQL (mirror of §9, six tables back to original names), then re-run pre-rename verification. Explicit statement: 'Run §9 rename only after the 24-48h watch passes. This keeps rollback to a single toggle flip during the watch window.'
  Depends on: T4
  Executor notes: §7: Symptom|Threshold|Action|Pointer markdown table covering 15 rows. Partial-publish split into two rows: (1) single partial in cutover window → INVESTIGATE via §6(b); (2) '(Phase-1 telemetry shadow partials) + (Phase-2 prod partials from §6) cumulative ≥ 2 per :600-601 → ABORT + restore topic_publications + §8.' Rejection rate >50%/hr from §6(d) → TUNE then ABORT consult redesign citing :676. Collision-override rate >10% → TUNE per :603/:670/:673. Duplicate posts split into two rows (1 isolated → HOT-FIX check _publish_accepted_candidates idempotency at :84-107 + topic_sources uniqueness per :625; ≥2 recurring → ABORT). Legacy editor accidentally publishes → HOT-FIX re-verify toggle + branch-A/B signal from §5. Discord 429s/auth errors → HOT-FIX routing. LEASE SYMPTOMS SPLIT INTO THREE DISTINCT ROWS per :684: (i) new-editor self-contention on (environment, guild_id, live_channel_id) → HOT-FIX release lease + verify next tick; (ii) legacy editor writing to NEW tables (topics, topic_*) → ABORT (isolation broke per :651/:684); (iii) new + legacy run rows overlapping on same lease key → ABORT (lease misconfigured per :684). Shape divergence vs Phase 1 baseline split: explainable → ACCEPT pointing to §4; unexplained → TUNE/ABORT. record_observation <1/run sustained → INFO post-cutover concern per :602. Third-mechanism row: 'Phase 1 turns out to be a third mechanism (not Branch A or B) OR run-source discriminator (TODO(phase-1) f) absent → ABORT immediately — this runbook does not cover the cutover.' Footer: 'If a symptom is not listed, ABORT to rollback and consult docs/live-update-editor-redesign.md.' §8: two variants. Pre-rename rollback (5 steps): (1) flip <LIVE_EDITOR_PUBLISH_TOGGLE> back to legacy value; (2) drain new-editor in-flight run OR force-release lease for (environment, guild_id, live_channel_id) per :684; (3) wait for next scheduler tick (hourly per :633), confirm legacy publishes via _publish_accepted_candidates (live_update_editor.py:84-107 called from :261-346) — Branch A: new legacy run row appears under legacy run-source discriminator (TODO(phase-1) f); Branch B: post-rollback legacy run rows show suppression marker CLEARED (if Phase 1 writes one — TODO(phase-1) c) AND new live-channel post attributed to legacy bot identity (TODO(phase-1) d) as primary Discord-side signal; (4) verify new live_update_feed_items row AND corresponding Discord post; (5) 'Leave topics rows alone — do not truncate, do not rewrite' citing :596 with rationale that the runner-pointer flip is the rollback and new-editor topics state is durable for post-mortem. Post-rename rollback (rare): same toggle flip + reverse-rename migration mirror of §9 (six tables _legacy_<name> back to original names) authored and handed off via the same .migrations_staging/ → supabase/migrations/ → supabase db push → verify in supabase_migrations.schema_migrations chain documented in §9; then re-run pre-rename verification. Explicit statement at top of §8: 'Run §9 rename only after the 24-48h watch passes. This keeps rollback to a single toggle flip during the watch window.' Escalation: if rollback fails to restore legacy publishing within two scheduler ticks, page POM and consult the redesign doc.
  Reviewer verdict: Pass. §7 splits lease symptoms into three distinct rows and includes a third-mechanism / discriminator-absent ABORT row; §8 leaves topics rows alone citing :596 and distinguishes pre-rename from post-rename rollback variants with Branch-A and Branch-B verification.
  Evidence files:
    - docs/live-update-editor-phase-2-runbook.md

- [x] **T6:** Author §9 (table-rename migration SQL — post-watch) and §10 (deletion schedule + close-out). §9: Migration-runner path explicitly: author migration as new timestamped file in .migrations_staging/ (e.g. 2026MMDDHHMMSS_live_update_legacy_rename.sql, matching convention of 20260511110349_live_update_environment_split.sql and 20260512185328_live_update_watchlist_lifecycle.sql); hand off per .migrations_staging/README.md — copy file VERBATIM (filename unchanged so ordering preserved) into /Users/peteromalley/Documents/banodoco-workspace/supabase/migrations/; apply via `supabase db push` from workspace repo (TODO(phase-1) if Phase 1 chose a different applier such as Supabase dashboard SQL editor or psql-direct against prod; SQL body identical regardless); verify migration recorded in supabase_migrations.schema_migrations before treating rename as applied. Ordered ALTER TABLE ... RENAME TO block in a begin; ... commit; transaction for six legacy tables per docs/live-update-editor-redesign.md:212-223 (cross-checked against src/common/storage_handler.py:652-1968 — NOT docs/live-update-editor-legacy-boundaries.md which is about daily_summaries): live_update_feed_items, live_update_watchlist, live_update_duplicate_state, live_update_editorial_memory, live_update_candidates, live_update_decisions → _legacy_<name>. Inline comment: live_update_editor_runs stays (:223, :562); swapping commit; for rollback; gives dry-run preview. Constraint-preservation note: Postgres preserves indexes, sequences, FK constraints, RLS across RENAME TO — do not recreate. Dependent views NOT auto-updated — pre-check `select schemaname, viewname from pg_views where definition ilike '%live_update_%';` and recreate or drop explicitly. Row-count parity SELECT before AND after (union all over six tables); counts must match exactly. Record _legacy_* deletion date inline: __________ (operator picks 2-4 weeks from today per :653). Do NOT hardcode date. §10: 'Before deletion date fires' checklist (no recent reads from _legacy_*, no open incidents referencing legacy data, POM explicit sign-off). DROP TABLE _legacy_<name>; block for all six tables in single transaction, authored via same .migrations_staging/ → supabase/migrations/ → supabase db push hand-off as §9. Bring-forward circumstances (storage pressure, schema-drift complications, audit done early); push-out circumstances (active forensics, regulator request, ongoing post-cutover partial-publish investigation). Close-out checklist: legacy dropped, runbook archived, redesign doc updated to note Phase 2 done.
  Depends on: T5
  Executor notes: §9 opens with 'Run this only after the 24-48h watch has passed cleanly and §8 will not be needed.' Hand-off chain (4 numbered steps) names in full: (1) author timestamped file in .migrations_staging/ as 2026MMDDHHMMSS_live_update_legacy_rename.sql matching existing convention 20260511110349_live_update_environment_split.sql and 20260512185328_live_update_watchlist_lifecycle.sql; (2) copy file VERBATIM filename-unchanged into /Users/peteromalley/Documents/banodoco-workspace/supabase/migrations/ per .migrations_staging/README.md; (3) apply via `supabase db push` from workspace repo with TODO(phase-1) note that Phase 1 may have chosen a different applier (Supabase dashboard SQL editor or psql-direct) and SQL body is identical regardless; (4) verify migration recorded in supabase_migrations.schema_migrations with a SELECT query example before treating rename as applied. Pre-flight constraint-preservation note: 'Postgres preserves indexes, sequences, FK constraints, and RLS across ALTER TABLE ... RENAME TO — do not recreate them. Dependent views are NOT auto-updated.' Pre-check view query `select schemaname, viewname, definition from pg_views where definition ilike '%live_update_%';` with instruction to drop/recreate any matching views. Row-count parity SELECT before AND after as 6-table UNION ALL (counts must match exactly). Migration body: begin;...commit; transaction with ordered ALTER TABLE ... RENAME TO for six legacy tables per docs/live-update-editor-redesign.md:212-223 (cross-checked against src/common/storage_handler.py:652-1968 — NOT docs/live-update-editor-legacy-boundaries.md which is about daily_summaries): live_update_feed_items, live_update_watchlist, live_update_duplicate_state, live_update_editorial_memory, live_update_candidates, live_update_decisions → _legacy_<name>. Inline comment 'live_update_editor_runs is NOT renamed (still used by both editors per :223/:562)' and 'swap to `rollback;` for dry-run preview' at commit; line. Post-flight row-count UNION ALL against _legacy_* names. Operator-filled blanks: 'Rename applied on: __________' and '_legacy_* deletion scheduled for: __________' citing :653; explicit 'Do not hardcode a date in this doc'. §10 'Before the deletion date fires' as a 3-item YES checklist (zero recent reads, no open incidents, POM sign-off in writing). Bring-forward circumstances (storage pressure, schema-drift, audit done early). Push-out circumstances (active forensics, regulator request, ongoing partial-publish investigation). DROP migration block: 'Use the same hand-off chain as §9' authored as 2026MMDDHHMMSS_live_update_legacy_drop.sql → supabase/migrations/ → supabase db push → verify in supabase_migrations.schema_migrations. begin;...commit; with DROP TABLE _legacy_<name>; for all six tables and rollback comment for dry-run. Explicit 'live_update_editor_runs is not dropped — same reason as §9.' Close-out checklist: legacy dropped + DROP migration recorded; runbook archived (move to docs/archive/ or note Phase 2 complete at top); docs/live-update-editor-redesign.md updated to note Phase 2 done (date, partial-publish-incident count, override-rate observed); trip-wire counters from :599-604 re-checked against first week of post-cutover data.
  Reviewer verdict: Pass. §9 names the full migration hand-off chain (staging → workspace migrations → supabase db push → verify in supabase_migrations.schema_migrations), renames the correct six legacy tables, retains live_update_editor_runs, uses BEGIN/COMMIT with dry-run note, has pre/post row-count UNION ALL, and includes constraint-preservation note. §10 uses the same chain for DROP and leaves the deletion date as an operator-filled blank.
  Evidence files:
    - docs/live-update-editor-phase-2-runbook.md

- [x] **T7:** Author §11 (consult-the-doc escape hatch footer) and complete a self-review polish pass over the assembled document. §11: Short footer — every section that could fork on Phase-1 deviation defaults to ABORT-to-rollback. Re-list named redesign anchors (:117-123, :188-191, :212-223, :223, :596, :599-604, :600-601, :611, :627, :633, :642-644, :646-653, :651, :684). Re-list three trip-wire signals from :599-604: partial-publish ×2 cross-window (Phase-1 telemetry + Phase-2 topics) → ABORT + restore topic_publications; override rate >10% → TUNE; record_observation <1/run → drop tool (post-cutover concern). Self-review polish pass — verify: (1) all eight pass_to_pass expectations map to a section; (2) §6 trip-wire tally does NOT query topics WHERE environment='dev'; (3) §5 Branch B names Discord-side count as PRIMARY signal and SQL marker as nice-to-have; (4) §1 banner lists SIX TODO(phase-1) items including run-source discriminator and explicit out-of-scope clause for third mechanism; (5) §3 preconditions includes 'Phase 1 shipped Branch A or Branch B (not a third mechanism)' AND 'run-source discriminator present' as gates (all six TODOs resolved); (6) §7 decision tree includes third-mechanism row that also covers run-source-discriminator-absent; (7) §9 step 1 names full migration hand-off chain (.migrations_staging/ → supabase/migrations/ → supabase db push → verify in supabase_migrations.schema_migrations); (8) §10 step 2 uses same hand-off chain for DROP TABLE; (9) §5 verification anchors on next observed live_update_editor_runs row not fixed window; (10) §7 partial-publish threshold is '(Phase-1 telemetry) + (Phase-2 prod) cumulative ≥ 2' with :600-601 citation; (11) §7 lease symptoms split into three distinct rows; (12) §9 cross-checks against :212-223 and src/common/storage_handler.py:652-1968 NOT docs/live-update-editor-legacy-boundaries.md; (13) SQL is environment='prod' with `-- swap to 'dev'` comments and 'dev has no partials' annotation where it would mislead; (14) length ~400 lines (200-700 acceptable), schema referenced not duplicated, _legacy_* deletion date blank, every TODO(phase-1) visibly labeled. Verify anchor references in the doc resolve to real lines in source files.
  Depends on: T6
  Executor notes: §11 footer authored. Opens 'Every section that could fork on a Phase-1 deviation defaults to ABORT to §8 rollback and consult docs/live-update-editor-redesign.md.' Cross-references §1 out-of-scope, §3 final two preconditions, §5 Branch-A note, §7 last row. Lists all 14 named redesign anchors: :117-123, :188-191, :212-223, :223, :596, :599-604, :600-601, :611, :627, :633, :642-644, :646-653, :651, :684. Re-lists three trip-wire signals from :599-604: (1) partial-publish ×2 cross-window (Phase-1 telemetry + Phase-2 topics) → ABORT + restore topic_publications; (2) collision-override rate >10% → TUNE per :603/:670/:673; (3) record_observation <1/run sustained → drop tool (post-cutover concern per :602). Closes 'When in doubt, ABORT to §8 and re-read the redesign doc.' Self-review polish pass complete: (1) all pass_to_pass expectations map to sections; (2) §6 trip-wire tally uses prod-only count + Phase-1 telemetry sum — NO query against topics WHERE environment='dev' (verified by grep: the three 'environment=dev' matches are all warning text saying NOT to query this); (3) §5 Branch B names Discord-side count as PRIMARY and SQL marker as nice-to-have secondary; (4) §1 banner lists SIX TODO(phase-1) items (a)-(f) including run-source discriminator and explicit out-of-scope clause for third mechanism; (5) §3 preconditions includes 'Phase 1 shipped Branch A or Branch B (not a third mechanism)' AND 'Run-source discriminator present' as separate gates; (6) §7 decision tree includes third-mechanism row that explicitly covers run-source-discriminator-absent; (7) §9 step 1 names full migration hand-off chain (.migrations_staging/ → supabase/migrations/ → supabase db push → verify in supabase_migrations.schema_migrations); (8) §10 DROP block uses same hand-off chain; (9) §5 verification anchors on next observed live_update_editor_runs row not fixed window; (10) §7 partial-publish threshold is '(Phase-1 telemetry) + (Phase-2 prod) cumulative ≥ 2' with :600-601 citation; (11) §7 lease symptoms split into three distinct rows; (12) §9 cross-checks against :212-223 + src/common/storage_handler.py:652-1968 NOT docs/live-update-editor-legacy-boundaries.md; (13) SQL is environment='prod' with `-- swap to 'dev'` comments and 'dev has no partials' annotation; (14) length 676 lines (within 200-700 acceptable), schema referenced via anchors not duplicated, _legacy_* deletion date is operator-filled blank, 26 TODO(phase-1) markers visibly labeled across the doc. Anchor references resolve to real lines in source files (verified in T1 audit).
  Reviewer verdict: Pass. §11 footer lists 14 named redesign anchors and re-states the three trip-wire signals from :599-604. Self-review polish pass items all met.
  Evidence files:
    - docs/live-update-editor-phase-2-runbook.md

## Watch Items

- MUST add a SIXTH TODO(phase-1) for the run-source discriminator on live_update_editor_runs — table today (per src/common/storage_handler.py:652-682) has no run_source/editor_source/run_type column. §1 banner, §3 precondition list, §5 Branch A note, and §7 third-mechanism row all need this enumerated explicitly (gate warnings #1, #2, #3).
- Output path is exactly docs/live-update-editor-phase-2-runbook.md — verify the file does not exist before writing.
- Tone: terse, imperative, copy-paste SQL ready — match docs/runbook-payments.md, NOT general prose. Length budget ~400 lines (200-700 acceptable).
- All SQL scoped to environment='prod' with `-- swap to 'dev' for pre-flip verification` comments; dev annotation must note 'dev has no partials during Phase 1 per :611/:627/:633' where the operator might misread '0 dev rows' as 'dev clean.'
- §5 Branch B PRIMARY verification signal is Discord-side count (scroll live channel for posts attributed to legacy bot identity since flip timestamp). SQL suppression marker is demoted to nice-to-have. Do NOT promote SQL marker over Discord count.
- §5 verification anchors on the NEXT observed live_update_editor_runs row (poll until new completed row appears) — NOT a fixed 15-minute window (cadence is hourly per :633).
- §5 verification does NOT check 'zero new rows in legacy live_update_feed_items' — legacy table writes continue during the watch per :651. Only Discord-send is suppressed.
- §6 partial-publish trip-wire is TWO-SOURCED: prod-only SQL count from topics + Phase-1 shadow telemetry count from trace embed + shadow logs. Sum ≥ 2 → ABORT. Do NOT query topics WHERE environment='dev' (vacuous by design).
- §7 partial-publish row threshold is '(Phase-1 telemetry shadow partials) + (Phase-2 prod partials from topics) cumulative ≥ 2 → ABORT + restore topic_publications' citing :600-601. Phrase exactly.
- §7 lease symptoms must split into THREE distinct rows: new-editor self-contention HOT-FIX; legacy writing to NEW tables ABORT; new+legacy run-row overlap on same lease key ABORT.
- §7 must include a row for 'Phase 1 turns out to be a third mechanism (not Branch A or B) OR run-source discriminator absent → ABORT immediately.'
- §9 migration hand-off chain MUST be named in full: author in .migrations_staging/ (timestamped filename matching convention) → copy verbatim to /Users/peteromalley/Documents/banodoco-workspace/supabase/migrations/ → apply via `supabase db push` → verify in supabase_migrations.schema_migrations. §10 DROP block uses same chain.
- §9 six-table cross-reference is docs/live-update-editor-redesign.md:212-223 AND src/common/storage_handler.py:652-1968 — NOT docs/live-update-editor-legacy-boundaries.md (that section is about daily_summaries, not the live_update_* tables).
- §9/§10: live_update_editor_runs stays (NOT renamed, NOT dropped) per :223 and :562.
- Rename runs ONLY after the 24-48h watch passes — so rollback during the watch is a single toggle flip. State this explicitly.
- §10 _legacy_* deletion date is an operator-filled blank, NOT hardcoded.
- Use <LIVE_EDITOR_PUBLISH_TOGGLE> as the placeholder name throughout. Reference _publish_accepted_candidates (src/features/summarising/live_update_editor.py:84-107) and _select_publishable_candidates (:261-346) as the legacy fallback path.
- Reference docs/live-update-editor-redesign.md anchors for schema shape — do NOT re-list column definitions in the runbook.
- Doc-only scope. Make NO code edits, NO test changes, NO file changes outside docs/live-update-editor-phase-2-runbook.md.
- Avoid scope creep: sentence-level adherence to the plan. Do not invent additional sections beyond the 11 specified.

## Sense Checks

- **SC1** (T1): Did the executor verify the output path is unused and re-read all named source anchors (especially confirming live_update_editor_runs lacks any run_source/editor_source/run_type column today)?
  Executor note: Confirmed: output path docs/live-update-editor-phase-2-runbook.md does not exist. All named source anchors re-read — redesign.md :110-223 and :580-697 covering :117-123/:188-191/:212-223/:596/:599-604/:611/:627/:633/:642-644/:646-653/:651/:684; storage_handler.py :652-682 confirms live_update_editor_runs has zero run_source/editor_source/run_type columns (only guild_id, environment, trigger, status, live_channel_id, checkpoint_before/after, memory_snapshot, duplicate_snapshot, *_count, skipped_reason, error_message, metadata, completed_at); live_update_editor.py :60/:84-107/:261-346 verified for legacy publish wiring; .migrations_staging/README.md confirms hand-off destination /Users/peteromalley/Documents/banodoco-workspace/supabase/migrations/ with copy-verbatim filename convention; runbook-payments.md :1-92 reviewed for tone. Sixth TODO(phase-1) for the run-source discriminator is required and will be enumerated in §1, §3, §5 Branch A, and §7.
  Verdict: Confirmed. Output path was unused; anchors re-read; the absence of any run_source/editor_source/run_type column on live_update_editor_runs justifies the sixth TODO(phase-1).

- **SC2** (T2): Does §1's provisional banner enumerate SIX TODO(phase-1) items including the run-source discriminator, with explicit out-of-scope clause for any third mechanism? Does §2 Fast Triage SQL group by publication_status scoped to environment='prod' with the dev-swap-comment + 'no partials in dev' annotation?
  Executor note: Confirmed: §1 provisional banner enumerates SIX TODO(phase-1) items labeled (a) through (f), with (f) being the run-source discriminator on live_update_editor_runs (citing src/common/storage_handler.py:652-682 to confirm no such column today). Explicit out-of-scope clause covers any third mechanism (hybrid, per-guild config, per-channel rollout, partial-guild canary) OR omission of TODO(f) → ABORT to §11. §2 Fast Triage SQL groups by publication_status scoped environment='prod' over last hour, with `-- swap to 'dev' for pre-flip verification` comment AND the 'no partials in dev during Phase 1 per :611/:627/:633' annotation that also warns operators not to misread '0 dev partials' as 'prod clean'.
  Verdict: Confirmed. §1 banner lists six TODO(phase-1) items with explicit out-of-scope clause; §2 Fast Triage SQL is prod-scoped with the correct annotations.

- **SC3** (T3): Does §3 preconditions include both 'Phase 1 shipped Branch A or Branch B (not a third mechanism)' AND the run-source-discriminator gate (i.e. all SIX TODOs resolved)? Does §4 GO/TUNE/ABORT rubric source Phase-1 shadow partials from Phase-1 telemetry (NOT from topics WHERE environment='dev')?
  Executor note: Confirmed: §3 preconditions table includes both 'Phase 1 shipped Branch A or Branch B (not a third mechanism)' as one gate AND 'Run-source discriminator present' as a separate gate, explicitly noting all SIX Phase-1 TODOs must be resolved (the previous row in the table enumerates a-f including (f) run-source discriminator). Explicit ABORT-to-§11 for third mechanism OR missing discriminator. §4 rubric annotates the partial-publish row 'Sourced from Phase-1 telemetry (trace embed in Phase-1 monitoring channel + shadow logs per :611-644), NOT from topics WHERE environment=dev — Phase 1 runs with publishing OFF per :611/:627/:633.' The shadow-attempted-publish partial count is a telemetry counter, never an SQL query against topics in dev.
  Verdict: Confirmed. §3 includes both Branch A/B gate and run-source-discriminator-present gate; §4 partial-publish row annotated to source from Phase-1 telemetry not topics WHERE environment='dev'.

- **SC4** (T4): Does §5 Branch B name Discord-side count as PRIMARY (with SQL marker as nice-to-have secondary), anchor verification on the next observed live_update_editor_runs row not a fixed window, and avoid checking 'zero new rows in legacy live_update_feed_items'? Does §6 use a prod-only trip-wire SQL count plus explicit instruction to add the Phase-1 telemetry shadow partial count for the sum-of-two-sources trip-wire?
  Executor note: Confirmed. §5 Branch B labels 'Primary signal (Discord-side count)' explicitly and demotes the SQL suppression marker to 'Secondary, nice-to-have' with fallback wording 'If no marker shipped, the Discord-side count is the only signal — that is acceptable.' Step 4 anchors on the next observed live_update_editor_runs row (poll until status='completed' appears, ~90 min hourly cadence per :633) — not a fixed 15-min window. Explicit 'Do not' statement bans the 'zero new rows in legacy live_update_feed_items' check, citing :651. §6 uses the prod-only SQL count `select count(*) as prod_partials_since_cutover from topics where environment='prod' and publication_status='partial' and last_published_at >= '<cutover-flip-timestamp>';` then explicit instruction to add the Phase-1 shadow-attempted-publish partial count from telemetry (NOT from topics WHERE environment='dev'). Trip-wire phrasing 'fires when sum ≥ 2 per :600-601 → ABORT (§8) + restore topic_publications.'
  Verdict: Confirmed. §5 Branch B names Discord-side count as PRIMARY and SQL marker as secondary; anchors on next observed run; explicit ban on legacy live_update_feed_items check. §6 trip-wire is prod-only SQL + Phase-1 telemetry instruction.

- **SC5** (T5): Does §7 split lease symptoms into three distinct rows, frame the partial-publish trip-wire as '(Phase-1 telemetry) + (Phase-2 prod) cumulative ≥ 2 → ABORT + restore topic_publications' citing :600-601, and include a third-mechanism row covering the run-source-discriminator-absent case? Does §8 leave topics rows untouched, distinguish pre-rename rollback (toggle flip) from post-rename rollback (reverse rename), cite :596, and cover both Branch-A and Branch-B legacy-publish-resumption verification?
  Executor note: Confirmed. §7 splits lease symptoms into THREE DISTINCT ROWS: (i) new-editor self-contention HOT-FIX release lease for (environment, guild_id, live_channel_id) + verify next tick; (ii) legacy editor writing to NEW tables (topics, topic_*) → ABORT (isolation broke per :651/:684); (iii) new + legacy run rows overlapping on same lease key → ABORT (lease misconfigured per :684). Partial-publish trip-wire phrased exactly as '(Phase-1 telemetry shadow partials) + (Phase-2 prod partials from §6) cumulative ≥ 2 per :600-601 → ABORT + restore topic_publications' with pointer to §6 trip-wire and §8. Third-mechanism row explicitly covers 'Phase 1 turns out to be a third mechanism (not Branch A or B) OR run-source discriminator (TODO(phase-1) f) absent → ABORT immediately.' §8 leaves topics rows alone (step 5: 'Leave topics rows alone — do not truncate, do not rewrite' citing :596). Pre-rename rollback (single toggle flip + drain + next-tick verify) is distinct from post-rename rollback (toggle flip + reverse-rename migration via the §9 hand-off chain + re-run verification). Branch-A verification (new legacy run row under legacy run-source discriminator on next tick) and Branch-B verification (suppression marker CLEARED + new Discord post by legacy bot identity as primary signal) are both covered explicitly. :596 cited at the leave-topics-alone instruction.
  Verdict: Confirmed. §7 splits lease symptoms into three rows; partial-publish trip-wire phrased exactly as required citing :600-601; third-mechanism row covers run-source-discriminator-absent. §8 leaves topics rows alone citing :596; pre- and post-rename rollback are distinct with both Branch A and Branch B verification.

- **SC6** (T6): Does §9 name the full migration hand-off chain (author in .migrations_staging/ → copy verbatim to /Users/peteromalley/Documents/banodoco-workspace/supabase/migrations/ → supabase db push → verify in supabase_migrations.schema_migrations), rename all six legacy tables to _legacy_<name>, retain live_update_editor_runs, run inside BEGIN/COMMIT with dry-run rollback note, include row-count parity before/after, and explicitly cross-check the six-table set against :212-223 + src/common/storage_handler.py:652-1968 (NOT docs/live-update-editor-legacy-boundaries.md)? Does §10 use the same hand-off chain for DROP TABLE and leave the deletion date as an operator-filled blank?
  Executor note: Confirmed. §9 names the full migration hand-off chain in 4 numbered steps: (1) author in .migrations_staging/ with timestamped filename matching the 20260MMDDHHMMSS_*.sql convention (existing examples 20260511110349_live_update_environment_split.sql and 20260512185328_live_update_watchlist_lifecycle.sql verified via `ls`); (2) copy verbatim filename-unchanged to /Users/peteromalley/Documents/banodoco-workspace/supabase/migrations/; (3) apply via `supabase db push`; (4) verify in supabase_migrations.schema_migrations. Renames all SIX legacy tables (live_update_feed_items, live_update_watchlist, live_update_duplicate_state, live_update_editorial_memory, live_update_candidates, live_update_decisions) to _legacy_<name>. Retains live_update_editor_runs with explicit inline comment 'NOT renamed (still used by both editors per :223/:562).' Runs inside BEGIN/COMMIT with `swap to rollback; for dry-run preview` note. Constraint-preservation note explicit (Postgres preserves indexes/sequences/FK/RLS across RENAME TO; dependent views NOT auto-updated). Pre-check pg_views query included. Row-count parity SELECT before AND after as 6-table UNION ALL. Six-table cross-reference is :212-223 + src/common/storage_handler.py:652-1968 — explicitly NOT docs/live-update-editor-legacy-boundaries.md (with reason 'that section is about daily_summaries'). §10 uses the SAME hand-off chain for DROP TABLE (.migrations_staging/ → supabase/migrations/ → supabase db push → verify) and leaves the deletion date as an operator-filled blank ('Rename applied on: __________ ; _legacy_* deletion scheduled for: __________' with explicit 'Do not hardcode a date in this doc').
  Verdict: Confirmed. §9 names the full migration hand-off chain in four numbered steps; renames the six legacy tables to _legacy_<name>; retains live_update_editor_runs with inline comment; BEGIN/COMMIT with dry-run rollback note; pre/post row-count UNION ALL; constraint-preservation note; six-table cross-reference cites :212-223 and storage_handler.py:652-1968, explicitly NOT legacy-boundaries.md. §10 DROP uses the same hand-off chain and the deletion date is an operator-filled blank.

- **SC7** (T7): Does §11 footer point back to docs/live-update-editor-redesign.md anchors and re-list the three trip-wire signals from :599-604? Did the self-review confirm length ~400 lines, schema referenced not duplicated, all six TODO(phase-1) markers visibly labeled, and tone matches docs/runbook-payments.md?
  Executor note: Confirmed. §11 footer points back to docs/live-update-editor-redesign.md anchors with all 14 named anchors re-listed: :117-123, :188-191, :212-223, :223, :596, :599-604, :600-601, :611, :627, :633, :642-644, :646-653, :651, :684. Three trip-wire signals from :599-604 re-listed verbatim: (1) partial-publish ×2 cross-window (Phase-1 telemetry + Phase-2 topics) → ABORT + restore topic_publications; (2) collision-override rate >10% → TUNE (citing :603/:670/:673); (3) record_observation <1/run sustained → drop tool (post-cutover concern per :602). Self-review confirms: length 676 lines (within 200-700 acceptable range, ~70% above ~400 target but appropriate given six runbook-required SQL queries + 12-row precondition table + 15-row decision tree + migration body); schema referenced via redesign anchors throughout, not duplicated; six TODO(phase-1) markers a-f visibly labeled with 26 total occurrences across the doc; _legacy_* deletion date is operator-filled blank ('__________' with 'Do not hardcode a date' instruction); tone is terse imperative SQL-first matching docs/runbook-payments.md (Fast Triage section title, decision tables, copy-paste SQL blocks, no narrative prose).
  Verdict: Confirmed. §11 footer lists the 14 named redesign anchors and re-lists the three trip-wire signals from :599-604. Self-review polish pass items met: 676-line length is at upper bound of acceptable 200-700 range, six TODO(phase-1) markers visibly labeled, _legacy_* deletion date is an operator-filled blank, tone matches runbook-payments.md.

## Meta

Single-file doc authoring at docs/live-update-editor-phase-2-runbook.md. Mirror docs/runbook-payments.md tone — terse, imperative, copy-paste SQL ready, minimal narrative prose. Length budget ~400 lines (acceptable range 200-700). All schema details reference docs/live-update-editor-redesign.md anchors rather than re-listing columns.

CRITICAL EXECUTOR ADDITIONS from gate warnings (sentence-level, no new sections):
1. Add a SIXTH TODO(phase-1) to the §1 provisional banner: 'legacy/new run-source discriminator on live_update_editor_runs — column, metadata JSON key, or trigger-prefix convention; whichever Phase 1 chooses. live_update_editor_runs today (per src/common/storage_handler.py:652-682) has no run_source/editor_source/run_type column, so this is a Phase-1 add.'
2. Add a one-line note alongside §5 Branch-A verification: 'requires Phase-1-provided run-source discriminator (sixth TODO); if absent at flip time, treat as third-mechanism case and ABORT.'
3. §3 'all Phase-1 TODOs resolved' precondition must reference SIX TODOs (not five).
4. §7 third-mechanism decision-tree row should explicitly include 'or run-source discriminator absent' as a triggering condition.

OTHER KEY POINTS:
- §2 Fast Triage: scope to environment='prod' with last-hour window per plan Step 3.1.
- §6 partial-publish trip-wire is two-sourced: prod-only SQL count + Phase-1 shadow telemetry count, summed. Do NOT query topics WHERE environment='dev' — vacuous by design per :611/:627/:633.
- §5 Branch B PRIMARY signal = Discord-side count (scroll the live channel for posts attributed to legacy bot identity since flip timestamp). SQL suppression marker is nice-to-have secondary.
- §5 verification anchors on the NEXT observed live_update_editor_runs row with status='completed', not a fixed 15-minute window.
- §5 verification does NOT check 'zero new rows in legacy live_update_feed_items' — legacy table writes continue during the watch per :651.
- §9 migration hand-off chain: author in .migrations_staging/ (timestamped filename matching 20260MMDDHHMMSS_* convention) → copy verbatim to /Users/peteromalley/Documents/banodoco-workspace/supabase/migrations/ per .migrations_staging/README.md → apply via `supabase db push` → verify recorded in supabase_migrations.schema_migrations. §10 DROP uses same chain.
- §9 cross-checks against docs/live-update-editor-redesign.md:212-223 and src/common/storage_handler.py:652-1968 — NOT against docs/live-update-editor-legacy-boundaries.md (that section is about daily_summaries).
- §7 lease symptoms split into THREE rows (new-editor self-contention HOT-FIX; legacy writing to NEW tables ABORT; new+legacy run-row overlap on same lease key ABORT).
- §7 partial-publish row: '(Phase-1 telemetry shadow partials) + (Phase-2 prod partials from topics) cumulative ≥ 2 → ABORT + restore topic_publications' citing :600-601.
- §10 deletion date is operator-filled blank, NOT hardcoded.
- Use <LIVE_EDITOR_PUBLISH_TOGGLE> as the placeholder name throughout.
- Reference src/features/summarising/live_update_editor.py — specifically _publish_accepted_candidates (:84-107) and _select_publishable_candidates (:261-346) — as the legacy fallback path.

Settled decisions SD-1..SD-8 preserved. Doc-only scope — no code edits anywhere else.
