{
  "version": 2,
  "timestamp": "2026-05-14T10:16:41Z",
  "hash": "sha256:43b25c858fcb1f59f4940598c6f842e8be5c8844e925efa7be6cb1314d4d35fd",
  "changes_summary": "Resolved the critique by making the media-ref schema explicit for attachments and embeds, requiring read-tool media metadata, relaxing the blocks-only `post_sectioned_topic` guard, and ensuring archive-resolved source rows feed author and collision logic rather than only validation/publishing.",
  "flags_addressed": [
    "FLAG-001",
    "FLAG-002",
    "FLAG-003",
    "FLAG-004",
    "issue_hints",
    "correctness",
    "scope",
    "all_locations",
    "callers"
  ],
  "questions": [
    "Should legacy `body` and `sections` fields be preserved alongside `blocks` in newly stored structured summaries for rollback/debug readability, or should new structured writes store only `blocks`?"
  ],
  "success_criteria": [
    {
      "criterion": "Structured `post_sectioned_topic` inputs with `blocks` and no `sections` are accepted when blocks are valid.",
      "priority": "must",
      "requires": [
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "Structured topics store an ordered `topics.summary.blocks` document with block-level `source_message_ids` and canonical `media_refs`.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "Attachment media refs and embed media refs are both addressable, validated, and resolved deterministically at publish time.",
      "priority": "must",
      "requires": [
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "`get_message_context` exposes bounded agent-facing media metadata for attachments and embeds so archive messages can produce stable media refs.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "Archive-resolved source message rows feed `source_authors`, `post_simple_topic` author/source-count validation, and collision detection.",
      "priority": "must",
      "requires": [
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "The persisted topic-level `source_message_ids` and `topic_sources` rows equal the distinct union of all block-level source IDs.",
      "priority": "must",
      "requires": [
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "Structured document publishing renders inline linked source citations at each relevant block and does not append a global Sources footer.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_files"
      ]
    },
    {
      "criterion": "Publisher resolves Discord jump URLs from message metadata at publish time using `guild_id`, `channel_id`, and `message_id`.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_files"
      ]
    },
    {
      "criterion": "Publisher sends media URLs immediately after the block that owns each media ref.",
      "priority": "must",
      "requires": [
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "Invalid block source IDs or media refs are rejected with auditable transition errors rather than being silently omitted.",
      "priority": "must",
      "requires": [
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "Normal structured sectioned topics send one Discord message per text block, and paragraph-aware chunking is used only for blocks that exceed Discord limits.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_files"
      ]
    },
    {
      "criterion": "Existing simple-topic rendering and publishing behavior remains compatible unless the summary uses the new structured `blocks` shape.",
      "priority": "must",
      "requires": [
        "run_tests"
      ]
    },
    {
      "criterion": "The implementation keeps normalization, validation, rendering, metadata lookup, and Discord side effects separated into small helpers rather than embedding all logic in `_publish_topic`.",
      "priority": "should",
      "requires": [
        "read_files",
        "subjective_judgment"
      ]
    }
  ],
  "assumptions": [
    "`topics.summary` is JSON-capable and can store `blocks` without a database migration.",
    "Structured topics should use `summary.blocks` as canonical storage; legacy `body`/`sections` can be preserved only as compatibility/readability fields if useful.",
    "Canonical media refs use `{message_id, kind, index}` with `kind` as `attachment` or `embed`; `{message_id, attachment_index}` remains accepted shorthand for existing attachment-index workflows.",
    "Discord jump URLs should use `https://discord.com/channels/{guild_id}/{channel_id}/{message_id}` from archived message metadata.",
    "Media publishing remains URL-message based for this change, matching current behavior rather than uploading files.",
    "Archive-resolved source IDs are allowed when the metadata resolver finds them, and those resolved rows must feed validation, source authors, collisions, citations, and media hydration."
  ],
  "delta_from_previous_percent": 83.54,
  "structure_warnings": []
}
